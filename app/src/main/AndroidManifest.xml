<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    
    <!-- Alarm and reminder permissions for Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Required for alarms to work after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Required for alarms to wake device from sleep -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Foreground service permission for reliable background operations -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Full-screen intent to wake screen and show notification when screen is off/locked -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    
    <!-- Request to ignore battery optimizations for reliable alarms -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediTrackApp">

        <activity
            android:name=".HistoryMedicine"
            android:exported="false" />
        <activity
            android:name=".MedicineSchedule"
            android:exported="false" />
        <activity
            android:name=".MedicineListActivity"
            android:exported="false" />
        <activity
            android:name=".AddMedicine"
            android:exported="false" />
        <activity
            android:name=".MedicineType"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".Dashboard"
            android:exported="false" />
        <activity
            android:name=".Settings"
            android:exported="false" />
        <activity
            android:name=".signup"
            android:exported="false" />
        <activity
            android:name=".login"
            android:exported="false" />

        <!-- Extra location -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyATND4k8tfID_G2suoKxoMqz5cdW825SrQ" />

        <activity
            android:name=".NearByLocationActivity"
            android:exported="false" />
        <activity
            android:name=".SmsStatusActivity"
            android:exported="false" />
            
        <!-- Broadcast Receiver for notification actions -->
        <receiver
            android:name=".NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="MARK_AS_TAKEN" />
                <action android:name="SKIP_MEDICINE" />
            </intent-filter>
        </receiver>
        
        <!-- Broadcast Receiver for medicine alarms - works globally -->
        <receiver
            android:name=".MedicineAlarmReceiver"
            android:exported="false"
            android:enabled="true">
        </receiver>
        
        <!-- Boot Receiver to reschedule alarms after device restart -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- SMS sent/delivered status receivers -->
        <receiver
            android:name=".SmsSendReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="SMS_SENT" />
                <action android:name="SMS_DELIVERED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
